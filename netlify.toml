[build]
  command = "CI= npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"   # optional - உங்கள் project local-ல் எந்த node version use பண்ணுகிறீர்களோ அதையே lock பண்ணுங்க (16/18/20)
